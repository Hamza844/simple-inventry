pipeline {
    agent { label 'mynode' } 

    stages {
        stage('Hello World') {
            steps {
                echo 'Hello World from Jenkins on AWS Agent!'
            }
        }
    }
}
